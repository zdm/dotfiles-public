"%~dp0":
  trigger: "%~dp0"
  body: "%~dp0"
"%~sf0":
  trigger: "%~sf0"
  body: "%~sf0"
"%APPDATA%":
  trigger: "%APPDATA%"
  body: "%APPDATA%"
"%LOCALAPPDATA%":
  trigger: "%LOCALAPPDATA%"
  body: "%LOCALAPPDATA%"
"%ProgramData%":
  trigger: "%ProgramData%"
  body: "%ProgramData%"
"%ProgramFiles%":
  trigger: "%ProgramFiles%"
  body: "%ProgramFiles%"
"%USERNAME%":
  trigger: "%USERNAME%"
  body: "%USERNAME%"
"%USERPROFILE%":
  trigger: "%USERPROFILE%"
  body: "%USERPROFILE%"

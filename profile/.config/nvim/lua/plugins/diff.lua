return {
    {
        "sindrets/diffview.nvim",
        config = function ()
            -- local actions = require( "diffview.actions" )

            -- require( "diffview" ).setup( {} )
        end
    },
}
